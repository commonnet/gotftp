package gotftp
/*
import (
	"testing"
	"net"
)

func TestUDPServerConnect(t *testing.T) {
	run := true
	ip := "127.0.0.1"
	port := 8000

	addr := net.UDPAddr{
		Port: port,
		IP: net.ParseIP(ip),
	}

	go UDPServer(ip, port, &run)

	_, err := net.Dial("udp", addr.String())

	if err != nil {
		t.Error("unable to connect to udp server, got ", err)
	}

	run = false
}*/


